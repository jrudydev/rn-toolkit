import React from 'react';
import { Divider, DividerProps } from '@rn-toolkit/primitives';

/**
 * SDUI Divider component
 * Wraps @rn-toolkit/primitives Divider
 */
export function SDUIDivider(props: DividerProps) {
  return <Divider {...props} />;
}
